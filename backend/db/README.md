# Database

The initial relational schema is in `schema/edtech_platform.sql`, based on `docs/db design/DB_Design_Estimated_CSV.csv`.

Each microservice can own its migrations over time. For now this file gives you the shared starting structure.
